name: heart_disease_env
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
      - mlflow
      - scikit-learn
      - pandas
      - numpy
      - joblib
      - matplotlib
      - seaborn